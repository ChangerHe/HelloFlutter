import 'package:flutter/material.dart';

class TabEmailScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: Text('EMAIL'),
      // ),
      body: Center(
        child: Text('EMAIL'),
      ),
    );
  }
}
