import 'package:flutter/material.dart';

class TabAirPlayScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // appBar: AppBar(
      //   title: Text('AIRPLAY'),
      // ),
      body: Center(
        child: Text('AIRPLAY'),
      ),
    );
  }
}
