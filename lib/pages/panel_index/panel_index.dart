import 'package:flutter/material.dart';

class PanelIndex extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PanelIndex')
    );
  }
}