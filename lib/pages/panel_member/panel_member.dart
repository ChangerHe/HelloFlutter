import 'package:flutter/material.dart';

class PanelMember extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('PanelMember')
    );
  }
}