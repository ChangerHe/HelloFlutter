import 'package:flutter/material.dart';

class RightBackInnerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('安卓右滑返回'),),
      body: Center(
        child: Text('123'),
      ),
    );
  }
}
